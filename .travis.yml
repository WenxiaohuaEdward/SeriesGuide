language: android
env:
  matrix:
    - ANDROID_TARGET=android-22 
android:
  components:
    - tools
    - platform-tools
    - build-tools-23.0.3
    - android-22
    - extra-android-support
sudo: required
before_install:
    - chmod +x gradlew
script:
    - chmod +x ./gradlew   
after_success:
    - bash <(curl -s https://codecov.io/bash)
test:
    post:
        - if [ -e ./gradlew ]; then ./gradlew jacocoTestReport;else gradle jacocoTestReport;fi
        - bash <(curl -s https://codecov.io/bash) -t c429221f-dce8-4279-b113-d867fee6755a -WenxiaohuaEdward-c429221f-dce8-4279-b113-d867fee6755a